import React from 'react';
import { useDispatch } from 'react-redux';
import { logout } from '../../features/auth/authSlice';

const Dashboard: React.FC = () => {
  

  return (
    <div>
      <h1>Dashboard</h1>
      <p>Painel do utilizador em construção!</p>
      
    </div>
  );
};

export default Dashboard;